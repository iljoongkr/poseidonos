# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  vmImage: ubuntu-latest

variables:
  - group: azure

steps:
- script: |
    echo $(bloburl)
  displayName: 'variables test'

- script: |
    wget -q https://azcopyvnext.azureedge.net/release20211027/azcopy_linux_amd64_10.13.0.tar.gz
  displayName: 'download a file'

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Pipeline.Workspace)/s/azcopy_linux_amd64_10.13.0.tar.gz'
    artifact: 'drop'
    publishLocation: 'pipeline'
